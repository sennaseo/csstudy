# 🚀 csStudy AWS 배포 가이드 (초보자용)

폰에서도 쓰고, 진행상황도 어디서든 이어지게 만드는 전체 과정.
**총 3단계**야. 순서대로만 따라오면 돼. (예상 소요: 30~40분)

```
[1] DynamoDB  →  진행상황을 저장할 "금고"
[2] Lambda    →  금고를 열고 닫아주는 "창구 직원" (백엔드 API)
[3] Amplify   →  앱을 인터넷에 올리기 (프론트 호스팅)
```

> 💰 비용: 개인 사용량이면 전부 **프리티어 안**이라 사실상 0원이야.
> (DynamoDB 25GB, Lambda 월 100만 호출 무료 — 우리는 하루 몇십 번 수준)

---

## 1단계 — DynamoDB 테이블 만들기 (금고)

1. AWS 콘솔 로그인 → 상단 검색창에 **DynamoDB** → 「테이블 생성」
2. 입력값:
   - 테이블 이름: `csstudy`
   - 파티션 키: `pk` / 타입 **문자열(String)**
   - 나머지는 전부 기본값 그대로
3. 「테이블 생성」 클릭. 끝.

> 🧠 비유: 사물함 하나 만든 거야. 열쇠 이름표(`pk`)가 `state`인 칸 하나만 쓸 거야.

---

## 2단계 — Lambda 함수 만들기 (백엔드 API)

### 2-1. 함수 생성
1. 검색창에 **Lambda** → 「함수 생성」
2. 입력값:
   - 함수 이름: `csstudy-sync`
   - 런타임: **Node.js 22.x** (20.x도 OK)
   - 나머지 기본값 → 「함수 생성」

### 2-2. 코드 붙여넣기
1. 함수 화면의 「코드」 탭 → `index.mjs` 내용을 전부 지우고
2. 이 프로젝트의 **`server/lambda/index.mjs`** 파일 내용을 통째로 복사해서 붙여넣기
3. **「Deploy」 버튼** 클릭 (이거 안 누르면 저장 안 됨!)

### 2-3. 환경변수 설정
1. 「구성」 탭 → 「환경 변수」 → 「편집」 → 「환경 변수 추가」
2. 두 개 추가:

   | 키 | 값 |
   |---|---|
   | `TABLE_NAME` | `csstudy` |
   | `SYNC_TOKEN` | 아무 긴 랜덤 문자열 (예: `ym-cs-9f83kx2...`) |

   세 번째는 **선택**이야. 안 넣으면 코드에 적힌 기본값을 쓴다:

   | 키 | 값 |
   |---|---|
   | `ALLOWED_ORIGINS` | `https://sennaseo.github.io,http://localhost:5173` |

   > 🚪 이건 "어느 사이트가 이 API 를 부를 수 있나" 목록(CORS)이야.
   > 커스텀 도메인을 붙였을 때만 코드 수정 없이 여기서 바꾸면 돼.
   > 주소는 **끝에 슬래시 없이**, 경로(`/csstudy/`)는 빼고 도메인까지만 쓴다.

> 🔑 `SYNC_TOKEN`은 네가 만드는 **비밀번호**야. 길고 아무렇게나. 어디 적어둬 — 나중에 앱에 입력해야 해.

### 2-4. DynamoDB 접근 권한 주기
지금 Lambda(창구 직원)는 금고 열쇠가 없어. 열쇠를 주자:

1. 「구성」 탭 → 「권한」 → 실행 역할(role) 이름 클릭 (IAM 콘솔이 열림)
2. 「권한 추가」 → 「정책 연결」
3. `AmazonDynamoDBFullAccess` 검색 → 체크 → 「권한 추가」

> (더 엄격하게 하고 싶으면 csstudy 테이블만 허용하는 인라인 정책도 되지만, 개인 계정이면 이걸로 충분해.)

### 2-5. 함수 URL 만들기 (외부에서 부를 주소)
1. 「구성」 탭 → 「함수 URL」 → 「함수 URL 생성」
2. 인증 유형: **NONE** 선택
   - (토큰 검사는 코드 안에서 직접 하니까 NONE이어도 안전해)
3. **CORS 설정은 건드리지 마** (비활성 그대로 — 코드가 CORS 헤더를 직접 보내줘. 둘 다 켜면 헤더가 중복돼서 오히려 깨져)
4. 생성된 URL 복사 → 어디 적어둬. 이런 모양이야:
   `https://xxxxxxxx.lambda-url.ap-northeast-2.on.aws/`

### 2-6. 동작 테스트 (선택이지만 추천)
터미널(PowerShell)에서:

```powershell
curl -H "Authorization: Bearer 내토큰" https://내함수URL/
```

`{"data":null,"updatedAt":null}` 이 나오면 성공! (아직 저장된 게 없어서 null)
`{"error":"unauthorized"}` 가 나오면 토큰이 안 맞는 거야.

---

## 3단계 — 프론트 배포 (AWS Amplify Hosting)

GitHub에 코드를 올려두면, push할 때마다 자동으로 배포되는 방식. 제일 편해.

### 3-1. GitHub에 올리기 (아직 안 했다면)
```powershell
cd C:\Users\senna\Portfolio\csstudy
git init
git add .
git commit -m "csStudy"
# GitHub에서 새 저장소(private 추천) 만든 뒤:
git remote add origin https://github.com/내계정/csstudy.git
git push -u origin main
```

### 3-2. Amplify 연결
1. AWS 콘솔 검색창에 **Amplify** → 「새 앱 생성」→「웹 앱 호스팅」
2. **GitHub** 선택 → 권한 승인 → `csstudy` 저장소 + `main` 브랜치 선택
3. 빌드 설정은 자동 감지되는데, 아래처럼 되어 있는지 확인:
   - 빌드 명령: `npm run build`
   - 출력 디렉터리: `dist`
4. 「저장 및 배포」 → 몇 분 기다리면 끝!
5. `https://main.xxxxxx.amplifyapp.com` 같은 주소가 생겨. **이게 네 앱 주소야.**

> 이후로는 코드 고치고 `git push`만 하면 자동으로 새 버전이 배포돼.

---

## 4단계 — 폰에서 쓰기 🎉

1. 폰 브라우저로 Amplify 주소 접속
2. 앱 헤더의 **☁ 아이콘** 탭 → 함수 URL + 비밀 토큰 입력 → 「저장하고 연결」
3. 홈 화면에 추가 (PWA):
   - iPhone: Safari 공유 버튼 → 「홈 화면에 추가」
   - Android: Chrome 메뉴(⋮) → 「홈 화면에 추가」
4. 컴퓨터에서도 같은 주소 열고 ☁에 같은 값 입력 → **진행상황이 서로 이어져!**

> 동기화 타이밍: 앱 켤 때 / 다른 앱 갔다 돌아올 때 / 문제 풀고 2초 뒤 자동.

---

## ❓ 문제 해결 (FAQ)

**☁가 ⚠️로 바뀌어요**
→ 주소나 토큰 오타. 함수 URL 끝에 `/` 포함해서 그대로, 토큰은 Lambda 환경변수와 한 글자도 다르지 않게.

**브라우저 콘솔에 CORS 에러가 떠요**
→ 지금 앱이 뜬 주소가 허용 목록에 없다는 뜻. 콘솔 에러에 찍힌 Origin 을 그대로 복사해서
`ALLOWED_ORIGINS` 환경변수에 쉼표로 이어 붙이고 Lambda 를 다시 배포하면 돼.
(http/https, 포트까지 다르면 다른 주소로 친다)

**curl 테스트에서 500 에러**
→ 2-4 권한(DynamoDB 정책 연결)을 빼먹었을 가능성 99%. 또는 `TABLE_NAME` 오타.

**Amplify 빌드 실패**
→ 빌드 로그 확인. 대부분 출력 디렉터리가 `dist`가 아니게 설정된 경우.

**두 기기에서 동시에 풀면?**
→ 괜찮아. 합칠 때 "더 많이 한 쪽/더 최신인 쪽"이 남는 방식이라 기록이 사라지진 않아. 다만 같은 순간에 동시에 푸는 건 피하는 게 좋아.

**보안은?**
→ 개인용이라 토큰 하나로 지키는 구조야. 토큰만 남에게 안 알려주면 돼. (앱 주소 자체는 알려져도 데이터는 토큰 없이 못 봐)
